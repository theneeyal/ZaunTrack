name: my_flutter_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.6.0  # Updated to latest version
  firebase_storage: ^12.3.4  # Updated to latest version
  firebase_storage_web: ^3.10.3  # Updated to latest compatible version
  cloud_firestore: ^5.4.4  # Updated to latest version

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0  # Updated to latest version
  flutter_launcher_icons: ^0.14.1  # Updated to latest version

flutter_icons:
  android: true
  ios: true
  image_path: "assets/app_icon.png"

flutter:
  assets:
    - assets/app_icon.png
  uses-material-design: true
